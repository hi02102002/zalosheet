/**
 * Column title mappings for Google Spreadsheet
 * These correspond to the expected column names in the spreadsheet
 */
export const COLS_TITLE = {
  /** Phone number column */
  PHONE: 'phone',
  /** Message content column */
  CONTENT: 'content',
  /** Send time/status column (auto-populated by the bot) */
  SEND_TIME: 'sendTime',
}
